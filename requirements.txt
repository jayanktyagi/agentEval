# AgentEval dependencies
# Install with: pip install -r requirements.txt

# API framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Data validation
pydantic>=2.7.0
pydantic-settings>=2.0.0

# HTTP client — used to call agent endpoints
httpx>=0.27.0

# LLM — scenario generation
google-genai>=1.0.0

# Database — used in Week 2
sqlalchemy>=2.0.0
asyncpg>=0.29.0

# Job queue — used in Week 2
redis>=5.0.0

# Environment variables
python-dotenv>=1.0.0

# Optional — swap in when you have Anthropic credits
# anthropic>=0.28.0